import { ClassChangeColorPipe } from './class-change-color.pipe';

describe('ClassChangeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassChangeColorPipe();
    expect(pipe).toBeTruthy();
  });

});
